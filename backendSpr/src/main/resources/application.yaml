spring:
  application:
    name: backendSpr

  datasource:
    url: jdbc:postgresql://localhost:5432/typing_game
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true  
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format-sql: true 